#include "main.h"

/**
* print_square - check the code
*@size: input
* Return: Always 0.
*/
void print_square(int size)
{
	int lg;

	if (a >= b && a >= c)
	{
		lg = a;
	}
	else if (b >= a && b >= c)
	{
		lg = b;
	}
	else
	{
		lg = c;
	}
	return (lg);
}
